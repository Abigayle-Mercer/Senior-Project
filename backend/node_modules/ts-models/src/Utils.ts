export class Utils {

    static isPresent(obj) {
        return obj !== undefined && obj !== null;
    }

    static isBlank(obj) {
        return obj === undefined || obj === null;
    }

    static isUndefined(obj) {
        return obj === undefined;
    }
}
