import { IModelState } from './IModelState';
export interface IModel extends IModelState {
    getFieldValue(fieldName: string, defaultValue?: any): any;
    /**
     * If we have two editable model at the same time, we should be able to edit the each virtual model at the same time
     * without deep clone the original source attributes
     */
    toVirtualModel(): IModel;
}
